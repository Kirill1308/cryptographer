package org.javarush.cipher.incognito;

import org.junit.jupiter.api.Test;
import org.javarush.cipher.UserAction;

import static org.mockito.Mockito.*;

class IncognitoCipherStrategyTest {
//    @Test
//    void testExecuteEncrypt() {
//        // We mock the dependencies of IncognitoCipherStrategy
//        UserAction mockUserAction = mock(UserAction.class);
//        when(mockUserAction.toString()).thenReturn("ENCRYPT");
//
//        // We test the execute method
//        IncognitoCipherStrategy incognitoCipherStrategy = new IncognitoCipherStrategy(mockUserAction, "testfile.txt", 1);
//        incognitoCipherStrategy.execute();
//
//        // We verify that the execute method of the ActionCommand is called once
//        verify(mockUserAction, times(1)).execute();
//    }
//
//    @Test
//    void testExecuteDecrypt() {
//        // We mock the dependencies of IncognitoCipherStrategy
//        UserAction mockUserAction = mock(UserAction.class);
//        when(mockUserAction.toString()).thenReturn("DECRYPT");
//
//        // We test the execute method
//        IncognitoCipherStrategy incognitoCipherStrategy = new IncognitoCipherStrategy(mockUserAction, "testfile.txt", 1);
//        incognitoCipherStrategy.execute();
//
//        // We verify that the execute method of the ActionCommand is called once
//        verify(mockUserAction, times(1)).execute();
//    }
}